// One column grid for every row on Today, so a logged row and a loggable row
// line up number-for-number (docs/10-today-design.md): margin glyph, name,
// kcal, protein, trailing control.
export const ROW_GRID =
  "grid grid-cols-[1.25rem_minmax(0,1fr)_3.5rem_3rem_2.75rem] items-center gap-x-2";
